[build]
builder = "DOCKERFILE"
dockerfilePath = "packages/nocodb/Dockerfile"
dockerContext = "packages/nocodb"
buildCommand = ""

[deploy]
startCommand = ""
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
